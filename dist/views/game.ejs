<!DOCTYPE html>
<html>
<head>
    <title>Game</title>
    <style>
        #game-board{
            width:100%;
            height:auto;
            display: flex;
            flex-wrap: wrap;
            justify-content: space-evenly;
        }
        .card_img{
            width:100px;
            height:100px;
            border:solid 1px black;
            border-radius:10px;
        }
        img{
            width: 100px;
            height: 100px;
            object-fit: contain;
            opacity: 0;
            border:solid 1px black;
            border-radius:10px;
        }
    </style>
</head>
<body>
    <div id="game-board">
        <% for(let i = 0; i < board.length; i++) { %>
            <div class="card_img"><img src="<%= board[i] %>" class="card" id="card<%= i %>"></div>
        <% } %>
    </div>

    <script src="/game.js"></script>
</body>
</html>